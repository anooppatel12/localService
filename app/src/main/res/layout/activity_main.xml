<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="@color/background_color">

    <!-- Title Text -->
    <TextView
        android:id="@+id/select_role_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Your Role"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/primary_text"
        android:layout_marginBottom="32dp" />

    <!-- Service Provider Button -->
    <Button
        android:id="@+id/service_provider_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Service Provider"
        android:textSize="16sp"
        android:background="@drawable/rounded_button_provider"
        android:textColor="@color/white"
        android:layout_marginBottom="16dp"
        android:padding="12dp" />

    <!-- Service Taker Button -->
    <Button
        android:id="@+id/service_taker_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Service Taker"
        android:textSize="16sp"
        android:background="@drawable/rounded_button_taker"
        android:textColor="@color/white"
        android:padding="12dp" />
</LinearLayout>
